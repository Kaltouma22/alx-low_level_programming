#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @a: use for addition
 * @b: use for addition
 * Return: return (a + b)
*/

int add(int a, int b)
{
	return (a + b);
}
