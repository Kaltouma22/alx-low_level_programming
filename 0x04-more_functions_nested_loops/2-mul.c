#include "main.h"

/**
 * mul - checks for calcul the multiplication.
 *
 * @a: first input in function
 * @b: second input in function
 *
 * Return: return a * b
*/

int mul(int a, int b)
{
	return (a * b);
}
